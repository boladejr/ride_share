[project]
name = "rideshare-stripe-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "stripe>=8.0.0",
    "uvicorn[standard]>=0.30.0",
]

[tool.setuptools]
py-modules = ["main"]
